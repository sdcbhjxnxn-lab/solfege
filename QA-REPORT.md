# 视唱练耳训练台 — QA 验证报告

- **日期**：2026-09-24
- **对象**：`solfege`（视唱练耳训练台，VexFlow 4.2.3 + 原生 ES5 + Web Audio 实时合成）
- **范围**：全站 18 个路由的渲染与交互 + 谱面几何 + 旋律生成的乐理正确性
- **结论**：用户报告的「视唱谱面显示不全」已定位并修复；另发现并修复 **3 处同源/关联缺陷**，
  其中 1 处（节奏听辨选项完全没有音符）为**功能性缺陷**。三套回归（逻辑 / 结构 / 版式）全部通过。

---

## 一、结论摘要

| # | 严重度 | 缺陷 | 影响面 | 状态 |
|---|---|---|---|---|
| 1 | 高 | 视唱谱面下半部、谱号下沿与音符被 SVG 视口裁掉 | 谱例视唱 100% 命中 | 已修复 |
| 2 | **严重** | 节奏听辨四个选项谱面**完全没有音符**（音符被画到谱表下方约 200px） | 节奏听辨、节奏模仿 100% 命中 | 已修复 |
| 3 | 中 | 多小节按容器宽度均分，音符多的小节被压到 54px → 音符溢出谱表与视口 | 4 小节谱面、窄屏 | 已修复 |
| 4 | 中 | 含休止符的节奏型被原样透传成 `beats` 对象 → 小节时值变 `NaN`、音符退化为四分音符 | 高级难度的视唱谱例（约 3/9 节奏型命中） | 已修复 |

**根本原因高度收敛**：四处缺陷都源于**谱面渲染层缺少"几何量"层面的校验与约定**——
现有 5483 项 jsdom 断言只验证"DOM 里有没有元素"，而 SVG 会把越界元素**静默裁掉**，
于是"元素数量正确"与"画面对不上"可以同时成立。这是本次 QA 暴露出的**测试盲区**。

---

## 二、QA 方法

采用三层递进验证，各管一段，缺一不可：

| 层 | 手段 | 能发现 | 不能发现 |
|---|---|---|---|
| 逻辑层 | `node tools/test-notation.js`（jsdom + VexFlow 真实渲染） | 拼写、调号、音程/和弦/音阶数据错误 | 版式问题 |
| 结构层 | `node tools/test-e2e.js`（jsdom 加载真实 `index.html`） | 视图挂载、交互、事件、持久化 | **版式问题（jsdom 无布局）** |
| 版式层 | `bash tools/browser-measure.sh`（agent-browser 真实 Chromium） | **裁切、溢出、几何、音符落位** | — |

**关键判据（本次新增并固化）**：SVG 根元素默认 `overflow: hidden`，故必须比较
**内容真实包围盒（`getBBox()` 并集）与 `viewBox`**：

```js
const vb = svg.getAttribute('viewBox').split(/\s+/).map(Number);   // [x, y, w, h]
let maxY = -1e9, minY = 1e9;
svg.querySelectorAll('path,rect').forEach(el => { const b = el.getBBox();
  if (b.width || b.height) { maxY = Math.max(maxY, b.y + b.height); minY = Math.min(minY, b.y); } });
if (maxY > vb[3] + 0.5) console.warn('内容被裁', maxY - vb[3]);
```

---

## 三、缺陷详述

### 缺陷 1：视唱谱面被 SVG 视口裁掉（用户报告）

**现象**：谱例视唱的五线谱只显示上半部——第 4、5 条线、谱号下沿、全部音符的下面一半都不可见。

**根因**：`js/notation.js` 的版面高度公式把 VexFlow 的 `Stave(x, y, w)` 中的 `y` 当成了**第 0 条线**，
实际 `y` 是**谱表框顶部**，第 0 条线在 `y + 40`（VexFlow 4 默认 `space_above_staff_ln = 4` × `spacing = 10`），
谱表框自身高 90px。实测（`stave.getYForLine()`）：

```
Stave(0, 30, 400) → getYForLine(0..4) = 70, 80, 90, 100, 110   // 即 30 + 40 起
```

而旧公式 `height = (30 + 高于顶线级数*6) + 40 + 低于底线级数*6 + 24` 得出 **94**，
连第 4 条线（y=100）都装不下。实测 viewBox = `0 0 715 94`，内容包围盒 y ∈ [56, 127]，**越界 33px**。

**修复**（`js/notation.js:100-131`）：
- 新增 `geo(F)`：向 VexFlow 实测 `top / staff / step`（不硬编码常量，防小版本改默认值）；
- `staveYOf` / `layoutHeight` 按"顶线偏移 + 五线高 + 低音加线 + 谱号下沿留白"重算，且**以显式 `height` 为下限**；
- 新增 `fitSvgToContent()`（`js/notation.js:141`）作为兜底：渲染后按 `getBBox()` 反校 `viewBox`，
  确保任何元素都不被裁（jsdom 无 `getBBox`，静默跳过，不影响离线测试）。

**验证**：648 组参数组合（6 调性 × 3 拍号 × 3 小节数 × 3 难度 × 4 次随机）
→ **越界 0 处，音符视口内可见率 100%**（修复前为 94px 视口 / 127px 内容）。

### 缺陷 2：节奏听辨选项谱面完全没有音符（严重）

**现象**：节奏听辨题要求"从四个五线谱选项中选出正确的"，但四个选项的谱面上**一个音符都没有**，
只有谱号、拍号与小节线。

**根因**：`renderRhythm` 给非休止音符传 `midi: null`，而 `T.spellMidi(null)` 会算出 **C-1**
（`octaveOf(null) = -1`）。VexFlow 遂把音符头画到谱表下方 y≈290（视口高度仅 118），
并按"符干需触及中线"的规则拉出 **203px** 长符干——整条被 SVG 裁掉。

实测元素坐标（缺陷版）：

```
vf-stave      x=0   y=70 h=40          ← 谱表正常
vf-stavenote  x=74  y=90 h=210         ← 音符组高 210
vf-stem       x=88  y=90 h=203         ← 符干 203px
vf-notehead   x=77  y=290 h=10         ← 音符头落在 y=290
```

**修复**（`js/notation.js:334-355`）：非休止音符显式落在谱表中间线
（高音谱表 `B4 = midi 71` / 低音谱表 `D3 = midi 50`），并默认 `stemUp = true`。

**验证**：滚动 12 题 × 4 选项 → **期望音符数 == 实际渲染音符数、越界 0、最长符干 33px（正常值）**。

### 缺陷 3：多小节横向宽度均分导致音符溢出

**现象**：4 小节谱面时，第 2–4 小节被压到约 54px，音符横向溢出谱表与 SVG 视口（390px 窄屏下越界 36.6px）。

**根因**：旧分配 `firstW = max(150,(avail-60)/n) + firstExtra; restW = (avail-firstW)/(n-1)`，
首小节吃掉 250px，其余小节只能均分剩余，**与各小节实际音符数无关**。

**修复**（`js/notation.js:160-175` 新增 `systemLayout`）：按每小节音符数加权分配，
每小节设下限；容器装不下时整体加宽 SVG（由 CSS 等比缩放承接），**保证音符不溢出谱表**。

### 缺陷 4：休止符未等值替换，小节时值失真

**现象**：高级难度下部分视唱谱例的小节时值与拍号不符，播放时间轴为 `NaN`，音符时值退化为四分音符。

**根因**：`js/sight.js` 的 `rhythmChoices` 用 `filter(p => !isRest(p))` "过滤休止符"，
但 `p` 是**数组**（`isRest` 判断的是 `p.r !== undefined`），过滤条件恒为真——
休止符对象 `{r: 0.5}` 被**原样透传**成 `beats`：

```js
t += n.beats              // t = 0 + {r:0.5} → "0[object Object]" → totalBeats 变成字符串
beatsToDur({r:0.5})       // BEATS_MAP["[object Object]"] 未命中 → 回落为四分音符 'q'
```

命中数据（`js/theory.js:317-374`）：4/4 高级 `[{r:1},1,1,1]`、`[{r:0.5},0.5,1,1,1]`；
3/4 高级 `[{r:0.5},0.5,1,1]`，共 3/52 个节奏型。

**修复**（`js/sight.js:33-39`、`js/sight.js:75-77`）：
休止符按**自身时值等值替换**为音符（保持小节总时值不变），并在 `genMelody` 中
增加"每小节总时值 == 拍号拍数"的兜底校验。

**验证**：90 条旋律（3 拍号 × 3 难度 × 10 次随机）→ **小节时值不符 0 处**。

---

## 四、观察项与建议（未擅自改动）

1. **节奏听辨的提示语会泄题**：`js/trainers.js:454` 的提示直接给出正确拍号
   （"…，拍号 4/4"），而中/高级的四个选项混合了 4/4 与 3/4 节奏型（`js/trainers.js:416-423`），
   于是**不靠听辨、只看拍号即可排除错误项**，削弱了题型考查目的。
   建议二选一：(a) 提示语不暴露拍号；(b) 四个选项统一拍号，仅在节奏型上设干扰项。
   —— 属题型设计取舍，需你确认后我再改。
2. **窄屏 + 高密度谱面会整体缩小**：390px 手机上 4 小节 × 高级（约 17 个十六分音符）会缩到约 0.5 倍。
   已作为已知限制写入 README「七、已知限制」。实测最小音符间距：1280px 下 ≥ 3.8px；
   390px 极端情形相邻音符头可能相切（约 −1px，VexFlow 最小间距与音头字形宽度的差异）。
3. `assets/`、`css/`、`js/modules/` 三个目录为空且无任何引用，已删除。
4. 旧版 `shots/01..15.png` 仍是缺陷状态的截图，已用 `tools/browser-check.sh` 重新生成。

---

## 五、变更清单

**源码（2 个文件）**

| 文件 | 变更 |
|---|---|
| `js/notation.js` | 实测几何 `geo()`；重写 `staveYOf/layoutHeight`；新增 `fitSvgToContent()` 兜底；新增 `systemLayout()` 加权分配；`renderRhythm` 音符落中间线 + 符干向上 |
| `js/sight.js` | `rhythmChoices` 休止符等值替换；`genMelody` 增加小节时值兜底校验 |

**测试（2 个文件，均已验证"在缺陷版本上会失败"）**

| 文件 | 变更 |
|---|---|
| `tools/test-e2e.js` | 5483 → **5492** 项断言：节奏谱符干几何、谱表最小高度、视唱小节时值（含非数值时值判定） |
| `tools/browser-measure.sh` | 改造为**版式回归闸门**：31 项断言，全站裁切检测 + 648 组压力测试 + 音符数比对 + 小节时值 |

**其他**：`README.md`（新增「三·五 QA 回归指南」、更新工具说明与已知限制）；
`shots/01..15.png` 重新生成；删除 3 个空目录。

---

## 六、回归结果

```
node tools/test-notation.js     → ALL OK
node tools/test-e2e.js          → 通过 5492    失败 0    ✓
bash tools/browser-measure.sh   → 通过   31    失败 0    ✓（退出码 0）
```

版式层 31 项覆盖：18 个路由的 SVG 裁切检测、视唱 648 组参数组合、节奏听辨 10 题与视唱 6 条的音符数比对、
视唱 90 条旋律的小节时值、五度圈、移动端 420px 横向溢出、运行期错误。

**断言有效性反向验证**（关键——防止写出"永远通过"的空断言）：将 `js/notation.js` 与 `js/sight.js`
回退到缺陷行为后重跑 `tools/test-e2e.js`，新增断言如预期失败：

```
1. 节奏谱符干长度异常（音符落到了谱表之外）：203,203,203,203
2. 单谱表高度不足（会裁掉谱表下半部），实际 94
3. 视唱谱面存在非数值时值（休止符未被等值替换）：3/4:{"r":0.5}, ...
   视唱小节时值与拍号不符：3/4 第2小节=NaN拍
```

---

## 七、参考资料

- **VexFlow 4.2.3**（本项目内联，`vendor/vexflow.js`，约 992KB）：`Stave` / `StaveNote` / `Voice` /
  `Formatter` / `Beam` / `Dot` 的实际行为，均以本地实测（jsdom + 真实 Chromium）为准，非文档推测。
  上游仓库：<https://github.com/vexflow/vexflow>
- **难度分层依据**：中央音乐学院本科招生专业目录及考试大纲、海内外音乐基础知识测试题集
  —— 完整来源与链接见本项目 `README.md`「三、内容与难度的设计依据」。
- **SVG 规范**：SVG 2 中 `overflow` 的初始值是 `auto`，但**UA 样式表会把"非独立文档根元素"的 `<svg>`
  覆盖为 `hidden`**，此时 `viewBox` 之外的渲染内容被裁掉。本项目内联 `<svg>` 即属此类，
  实测 `getComputedStyle(svg).overflow === "hidden"`。
  <https://www.w3.org/TR/SVG2/render.html#OverflowAndClipProperties>
- **VexFlow API 可用性清单与全部踩坑记录**：已固化为 skill `vexflow4-notation`
  （含本次补充的「垂直几何」「SVG 视口裁切」「节奏谱禁止传 `midi: null`」三节）。
